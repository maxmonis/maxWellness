{
  "author": "Max Monis",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "config": "^3.2.6",
    "express": "^4.17.3",
    "express-validator": "^6.4.0",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^5.9.2"
  },
  "description": "MERN stack fitness tracker",
  "devDependencies": {
    "concurrently": "^7.0.0",
    "nodemon": "^2.0.2"
  },
  "engines": {
    "node": "14.x",
    "yarn": "1.x"
  },
  "keywords": [],
  "license": "ISC",
  "main": "server.js",
  "name": "max-wellness",
  "scripts": {
    "build": "yarn && cd app && yarn && yarn build",
    "dev": "concurrently \"yarn serve\" \"cd app && yarn start\"",
    "devsetup": "yarn && cd app && yarn",
    "serve": "nodemon server.js",
    "start": "node server.js"
  },
  "version": "1.1.0"
}
